# 《守线》联机部署与测试指南(M2)

> **当前状态(2026-07-18):公网服务器已上线并通过真人异地实测。** 客户端「联机对战」
> 面板已**内置默认服务器地址** `ws://212.64.21.174:5210/ws`(cicala.chat 那台 VPS,
> systemd 常驻、开机自启)——和朋友玩只需三步,见下方 §-1。
> §1–§3 的本机/局域网/Tailscale 路径保留,用于开发自测与无公网场景。

---

## -1. 和朋友联机(默认路径,零配置)

1. 双方拿到同一版本的游戏(`build/HoldTheLine-Windows-Demo.zip`,三件套勿拆)。
2. 一方:主菜单 →「联机对战」→ 选卡组 →「创建房间」→ 把 6 位**房间号**发给朋友。
3. 另一方:「联机对战」→ 选卡组 → 输入房间号 →「加入」。开打。

- 服务器地址框**不用动**(已默认公网服);想连本机/局域网服再手改。
- 中途掉线(切网/崩溃)在 **120 秒**内重启游戏重连同一局会自动续打;
  一方超时不操作,服务器 **90 秒**代为结束回合,连续两回合判负。
- 版本必须一致:服务器更新后旧客户端会被拒连(提示 version_mismatch),重新拿最新 zip 即可。

---

## 0. 组件速览

- **对战服务器**:`src/HoldTheLine.Server`,ASP.NET Core + WebSocket,监听 `0.0.0.0:5210`(可配)。
  - 健康检查:`GET /healthz` → `ok`。对战端点:`GET /ws`。
- **游戏客户端**:Godot 导出的 `HoldTheLine.exe`,主菜单「联机对战」填服务器地址即可。
- **陪练机器人**(可选,自测用):`src/HoldTheLine.BotClient`,能加入房间随机出牌打完一局。

配置项(环境变量 `HTL_` 前缀,或 `appsettings.json` 的 `Server` 段):

| 键 | 默认 | 说明 |
|---|---|---|
| `Server__Urls` | `http://0.0.0.0:5210` | 监听地址 |
| `Server__TurnSeconds` | `90` | 回合时限,超时服务器代打 EndTurn;同一玩家连续两回合超时判负 |
| `Server__DisconnectGraceSeconds` | `120` | 掉线宽限,窗口内用 resume_token 重连可续打;超时判掉线方负 |
| `Server__CommandLogDir` | 空(关闭) | 设为目录则每局落一份 `match-*.jsonl`(配置头 + 每条命令),可重放排查 |

---

## 1. 本机自测(最快,先跑通)

一台电脑上开服务器 + 一个 Godot 客户端 + 一个机器人陪练:

```bash
# 1) 起服务器
dotnet run --project src/HoldTheLine.Server -c Release
# 看到 "listening on http://0.0.0.0:5210" 即就绪;另开一个终端验证:
curl http://127.0.0.1:5210/healthz    # → ok

# 2) Godot 客户端:主菜单 → 联机对战 → 服务器填 ws://127.0.0.1:5210/ws → 创建房间 → 记下房间号

# 3) 机器人加入该房间陪你打(把 ABC123 换成上一步的房间号)
dotnet run --project src/HoldTheLine.BotClient -c Release -- --join ABC123 --play --url ws://127.0.0.1:5210/ws
```

千局压力自测(纯机器人对打,验证不崩不卡):

```bash
dotnet run --project src/HoldTheLine.BotClient -c Release -- --selfplay 1000 --url ws://127.0.0.1:5210/ws
```

---

## 2. 局域网(两台同 WiFi/网线)

服务器机查本机内网 IP(如 `192.168.1.20`),放行 5210 端口的入站:

```powershell
# 服务器机(Windows,管理员 PowerShell)一次性放行:
New-NetFirewallRule -DisplayName "HoldTheLine 5210" -Direction Inbound -Protocol TCP -LocalPort 5210 -Action Allow
```

- 服务器机:`dotnet run --project src/HoldTheLine.Server -c Release`(默认已监听 0.0.0.0)。
- 两台客户端主菜单「联机对战」服务器都填 `ws://192.168.1.20:5210/ws`,一人创建、一人用房间号加入。

---

## 3. 隔互联网:Tailscale 组网(推荐,零公网、零端口映射)

对 3~5 人测试最省事:你和朋友都装 [Tailscale](https://tailscale.com),登录同一账号(或用共享),
双方设备就进了同一个虚拟内网,彼此有稳定的 `100.x.y.z` 地址,不用买服务器、不用端口映射。

1. 双方安装并登录 Tailscale。服务器机跑 `dotnet run --project src/HoldTheLine.Server -c Release`。
2. 服务器机执行 `tailscale ip -4` 拿到它的 Tailscale IP(形如 `100.101.102.103`)。
3. 两台客户端「联机对战」服务器填 `ws://100.101.102.103:5210/ws`,创建/加入即可。
   - Windows 防火墙可能仍需放行 5210(见 §2 的命令);Tailscale 接口通常被视为专用网络。

> 这条路径即 M2 计划 §9 的默认方案(待定夺 D-M2-1)。打完 DoD 后若要长期在线,再考虑 §4 的 VPS。

---

## 4. 公网 VPS(已就绪:cicala.chat 那台腾讯云)

复用现有服务器 `root@212.64.21.174`(已跑 Portcialio 静态站 + PodEcho pm2 服务)。
部署完全隔离:自包含 linux-x64 发布(**服务器无需装 .NET**)、独立目录 `/opt/holdtheline`、
独立 systemd 服务 `holdtheline`,不触碰 nginx/pm2/现有项目。

**一键部署**(同 Portcialio 的部署模式):

```powershell
.\deploy\vps\deploy.ps1
# 发布 → 打包(server+卡表数据+systemd单元, ~42MB) → scp → 原子切换 → systemctl 重启 → healthz 自检
```

**首次部署前的唯一手动步骤**:腾讯云控制台 → 该实例安全组 → 放行 **TCP 5210 入站**
(nginx 只占了 80/443;5210 是新端口,控制台不开外网连不上)。

- 客户端服务器地址:`ws://212.64.21.174:5210/ws`
- 验证:浏览器开 `http://212.64.21.174:5210/healthz` 显示 `ok`
- 运维:`ssh root@212.64.21.174` 后 `systemctl status holdtheline` / `journalctl -u holdtheline -f`
- 每局命令日志落在服务器 `/var/lib/holdtheline/match-logs/*.jsonl`,可下载重放排查
- 可选加固(非验收项):加子域 `htl.cicala.chat` + certbot 走 `wss://`(cicala.chat 已备案,
  子域免单独备案;nginx 反代需加 WebSocket Upgrade 头)

> M2 不需要 Postgres/Redis(无账号无收藏);命令日志落本地 JSONL 即可。

---

## 5. 自测清单(对齐 M2 DoD,docs/05 §12)

- [x] **异地两台电脑经公网打完整局**,无崩溃、双方结算一致 ← 核心 DoD(2026-07-18 与朋友实测通过)
- [ ] **拔网线重连**:对局中把一方网络断开约 30 秒再恢复,客户端显示「重连中…」、对方看到
      「对手掉线,等待重连…」,恢复后无损续打(超过 `DisconnectGraceSeconds` 才判负)
      ← 唯一未人工验证项(自动化测试已覆盖),下次对局顺手测一把即可
- [x] **回合计时**:界面倒计时、超时代打、连续两回合判负(随异地实测生效)
- [x] **认输**:对局中按 Esc 认输,双方进入结算
- [x] 席位 1(加入方)视角镜像正确:己方底行在屏幕下方

> 自动化护城河已覆盖:两 bot 联网整局 + 裁剪断言 + 命令日志重放一致、断线重连、宽限判负、
> 超时判负、命令日志落盘重放(见 `HoldTheLine.Server.Tests`,14 条)。本清单是人工现场验收补充。

---

## 6. TLS 反代(wss,docs/12 B0)

密码登录(docs/12 B1)不走明文——**wss 是硬前置**。这台 VPS 的 **80/443 已被现有 nginx 占用**
(www/@/podecho 项目),所以用**现有 nginx** 反代 + certbot 签证书,**不装 Caddy、不动现有项目**。
(2026-07-19 实测确认:`ss -ltnp | grep -E ':80 |:443 '` 全是 nginx。)

**一次性配置**:

1. **⚠︎ 手动前置**:
   - DNS:给 `cicala.chat` 加 A 记录 `htl` → `212.64.21.174`;
   - 腾讯云安全组:80 + 443 已放行(现有 HTTP(80)/HTTPS(443) 规则即可,无需另加)。
2. 本机跑 `.\deploy\vps\setup-tls.ps1`:scp `deploy/vps/htl.nginx.conf` →
   `/etc/nginx/conf.d/htl.conf`,`nginx -t && systemctl reload nginx`。
3. 手动签证书:`ssh root@212.64.21.174` 后 `certbot --nginx -d htl.cicala.chat`
   (首次问邮箱/同意条款;自动往该 server 块加 443 SSL + 80→443 跳转)。没装则
   `apt install -y certbot python3-certbot-nginx`。
4. 验证:`curl -sf https://htl.cicala.chat/healthz` 显示 `ok`。

`/etc/nginx/conf.d/htl.conf`(仓库 `deploy/vps/htl.nginx.conf`)反代 `/ws` + `/healthz` 到
`127.0.0.1:5210`,带 WebSocket upgrade 头、`proxy_read_timeout 3600s`(回合制防空闲断链)、
`X-Forwarded-For`(服务器 `ForwardedHeaders` 读它,让 B1 按 IP 限流用真实客户端 IP 而非 127.0.0.1)。

- 客户端默认地址已是 `wss://htl.cicala.chat/ws`(`GameConfig.ServerUrl`);LAN 调试仍可手填 `ws://`。
- 明文 `ws://212.64.21.174:5210` 暂仍可用,**收口在 docs/12 B2.3**(届时服务器只监听
  `127.0.0.1:5210`,安全组移除 5210,公网只走域名 nginx)。
- 排障:`journalctl -u nginx` / `nginx -t`;证书 `/etc/letsencrypt/live/htl.cicala.chat/`,
  续期 `certbot renew --dry-run`。
